<Window x:Class="NexusContacts.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="450" Width="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="109*"/>
            <ColumnDefinition Width="158*"/>
            <ColumnDefinition Width="267*"/>
            <ColumnDefinition Width="267*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="איתן עוז בכר י&quot;א 4" 
                   Grid.Row="0" Grid.Column="3" 
                   HorizontalAlignment="Right" 
                   VerticalAlignment="Top" 
                   Margin="0,10,10,0" 
                   FontSize="14" 
                   Foreground="Gray"/>

        <TextBlock Text="Welcome to the contacts app" 
                   x:Name="Chetext"
                   FontSize="30" 
                   HorizontalAlignment="Center" 
                   Margin="0,20,0,20" 
                   Grid.Row="0" Grid.ColumnSpan="2" Grid.Column="1"/>
        <Button Content="set citys" Grid.Column="0" Grid.Row="0" Click="SetCity_Click" />
        <UniformGrid x:Name="ChoicesGrid" Grid.Row="1" Grid.ColumnSpan="4" Margin="20,20,20,20" Rows="3" Columns="0"/>
    </Grid>
</Window>